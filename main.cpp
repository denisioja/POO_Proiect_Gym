#include <iostream>
#include "Sala.h"

int main() {
    /*
    Sala *sala = new Sala;
    sala->citire(std::cin);
    sala->afisare(std::cout);
    sala->adaugareAngajat();
    sala->cautaClient("Popa");*/
    return 0;
}
